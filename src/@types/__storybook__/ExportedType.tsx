import React from 'react';
import { ExportedType as ExportedTypeType } from '..';

/**
 * PLEASE BE ADVISED:
 * These are defunct components used to present supplementary type information in the Search UI
 * storybook "docs" tabs. They are not referenced anywhere else in the library, so will not be
 * compiled or exported as part of Search UI.
 */
export const ExportedType: React.FC<ExportedTypeType> = () => null;
